# Core API framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.3

# Data validation
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Environment management
python-dotenv>=1.0.1

# HTTP client (async scraping calls)
httpx>=0.27.2

# HTML parsing
beautifulsoup4>=4.12.3
lxml>=5.3.0          # optional but recommended (faster parser; html.parser used as fallback)

# LangChain ecosystem
langchain>=0.3.7
langchain-groq>=0.2.1
langchain-community>=0.3.7
langchain-chroma>=0.1.4

# Embeddings
sentence-transformers>=3.2.1

# Vector store
chromadb>=0.5.18

# Scraping APIs
scrapingbee>=2.0.1
apify-client>=1.8.1

# PDF generation (xhtml2pdf is pure Python — no GTK/system libs required)
xhtml2pdf>=0.2.16
reportlab>=4.2.0
jinja2>=3.1.4

# Streamlit UI (Phase 1)
streamlit>=1.39.0
